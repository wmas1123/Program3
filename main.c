/**
 * This program will load an image file applies a filter and saves it back to the disk
 *
 * The image files accepted will be .bmp files
 *
 *
 * Completion Time: 1 hour
 *
 * @author William Mastronardi, Acuna
 * @version 1.0
 *
 * @TODO        -Create structures for the headers of a BMP file
 *              -Functions read and write a BMP file
 *              -Create structure that represents a single pixel
 *              -Accept an input file name given by user
 *              -Output file to a file optional save for the user
 *              -Validate inputs "-r -g -b"
 *              -Filter color shift on the image
 *              -Copy image BMP->BMP
 *              -Filter grayscale
 *              -Filter scaling
 *              -Image OOP
 *              -Scale given by user
 *
 */

#include <stdio.h>

int main() {
    printf("Hello, World!\n");
    return 0;
}
